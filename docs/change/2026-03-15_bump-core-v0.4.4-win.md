# 2026-03-15 - Win：对齐 Core v0.4.4 / SDK v0.1.6

## 变更背景 / 目标
- 背景：Core 已发布 `v0.4.4`（Windows RFCOMM 连接中止修复），SDK 已发布 `v0.1.6` 对齐该依赖。
- 目标：Win 发布链路对齐到最新依赖，保证 release 构建默认携带修复。

## 具体变更
- 修改 `go.mod`：
  - `github.com/yttydcs/myflowhub-core`：`v0.4.3` -> `v0.4.4`
  - `github.com/yttydcs/myflowhub-sdk`：`v0.1.5` -> `v0.1.6`

## 任务映射
- `WIN-1` 依赖升级
- `WIN-2` 测试验证
- `WIN-3` 文档归档
- `WIN-4` 提交合并与 tag

## 验证
- `go test ./... -count=1`：通过

## 影响与回滚
- 影响：仅升级依赖版本，不改 UI/业务逻辑。
- 回滚：回退 `go.mod` 与本归档文档。
