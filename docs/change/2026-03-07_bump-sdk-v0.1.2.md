# 2026-03-07 bump SDK v0.1.2（Win 依赖升级）

## 背景 / 目标

- 背景：SDK 发布 `v0.1.2`，`await` 增加对 VarStore 逐跳回程响应 `MajorCmd` 的严格白名单兼容（仅限 VarStore 子协议，避免误吞普通 Cmd）。
- 目标：Win 侧升级到 `myflowhub-sdk v0.1.2`，避免 VarStore/VarPool 等 await 路径在新协议下超时。

## 具体变更

- `go.mod`：`github.com/yttydcs/myflowhub-sdk v0.1.0 -> v0.1.2`
- `go.sum`：随依赖求解更新

## 测试与验证

- `GOWORK=off go mod tidy`（已执行）
- `GOWORK=off go test ./...`（已执行，通过）

## 回滚方案

- 将 `go.mod` 中 `myflowhub-sdk` 版本回退到 `v0.1.0`（或团队指定版本）并重新 `GOWORK=off go mod tidy`。
