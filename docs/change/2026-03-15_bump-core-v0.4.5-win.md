# 2026-03-15 - Win：对齐 Core v0.4.5 / SDK v0.1.7

## 变更背景 / 目标
- 背景：Core `v0.4.5` 修复 Windows RFCOMM 流式兼容问题，SDK `v0.1.7` 已同步对齐。
- 目标：Win 发行依赖统一到 `core v0.4.5` + `sdk v0.1.7`，用于蓝牙登录/注册回归验证。

## 具体变更
- 修改 `go.mod`：
  - `github.com/yttydcs/myflowhub-core`：`v0.4.4` -> `v0.4.5`
  - `github.com/yttydcs/myflowhub-sdk`：`v0.1.6` -> `v0.1.7`

## 任务映射
- `WIN-1` 依赖升级
- `WIN-2` 测试验证
- `WIN-3` 文档归档
- `WIN-4` 提交合并与 tag

## 验证
- `go test ./... -count=1`：通过

## 影响与回滚
- 影响：仅依赖版本升级，不改 Win 业务逻辑。
- 回滚：回退 `go.mod` 与本归档文档。
